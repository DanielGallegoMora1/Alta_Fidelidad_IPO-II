import 'package:flutter/material.dart';

class MapFeatureWidget extends StatelessWidget {
  const MapFeatureWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Map feature widget placeholder'));
  }
}
