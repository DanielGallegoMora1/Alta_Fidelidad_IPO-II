import 'package:flutter/material.dart';

class ProgressFeatureWidget extends StatelessWidget {
  const ProgressFeatureWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Progress feature widget placeholder'));
  }
}
